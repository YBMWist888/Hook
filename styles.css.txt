body {
    font-family: 'Lugrasimo', sans-serif;
    background: url('kalyan-dim-image.jpg') no-repeat center center/cover;
    background-size: cover;
    color: #fff;
    margin: 0;
    padding: 0;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
}

.header {
    text-align: center;
    padding: 50px 0;
    color: #fff;
    position: relative;
}

.header h1 {
    font-size: 50px;
    font-weight: bold;
}

.header p {
    font-size: 20px;
}

.content {
    padding: 20px;
    flex-grow: 1;
}

h2 {
    font-size: 36px;
    text-align: center;
    margin-top: 40px;
}

.menu-container {
    display: flex;
    justify-content: space-between;
    gap: 20px;
    margin-top: 40px;
}

.alcohol-section, .cocktail-section {
    width: 48%;
    padding: 20px;
    border-radius: 10px;
    background-color: transparent;
}

.alcohol-section h3, .cocktail-section h3 {
    font-size: 24px;
    margin-bottom: 20px;
}

.alcohol-list, .cocktail-list {
    list-style-type: none;
    padding: 0;
}

.alcohol-list li, .cocktail-item {
    background-color: rgba(0, 0, 0, 0.4);
    margin-bottom: 15px;
    padding: 15px;
    border-radius: 8px;
}

.cocktail-item img {
    width: 3cm;
    height: 5cm;
    object-fit: cover;
    border-radius: 8px;
    margin-right: 20px;
}

footer {
    background-color: #444;
    text-align: center;
    padding: 20px;
    color: #fff;
    position: fixed;
    width: 100%;
    bottom: 0;
}

/* Медіа-запити для мобільних пристроїв */
@media (max-width: 768px) {
    .menu-container {
        flex-direction: column; /* Вирівнюємо блоки по вертикалі */
    }

    .alcohol-section, .cocktail-section {
        width: 100%; /* Робимо секції по ширині екрану */
        margin-bottom: 20px;
    }

    .header h1 {
        font-size: 30px; /* Зменшуємо розмір заголовка для мобільних */
    }

    .header p {
        font-size: 16px; /* Зменшуємо підзаголовок */
    }

    .cocktail-item img {
        width: 100%; /* Робимо фото коктейлів на всю ширину екрану */
        height: auto; /* Автоматичне підстроювання висоти */
    }
}

/* Медіа-запити для планшетів */
@media (max-width: 1024px) {
    .menu-container {
        flex-direction: row; /* Для планшетів залишаємо горизонтальне розташування */
    }

    .cocktail-item img {
        width: 80%; /* Фото коктейлів більшого розміру на планшетах */
        height: auto;
    }
}
